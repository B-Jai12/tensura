/**
 * @tensura/ui-kit — Theme Constants (re-exported from @tensura/design-system)
 */

import { EMOJI, BRAND, NUMERIC_COLORS } from '@tensura/design-system';

export { EMOJI, BRAND };

// Re-export numeric colors under PALETTE name for backwards compatibility
export const PALETTE = NUMERIC_COLORS;
