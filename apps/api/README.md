# @tensura/api (not yet implemented)

Fastify + tRPC service — lands in Phase 11 per TENSURA_ARCHITECTURE.md.
Routers will mirror packages/core/src/modules 1:1, calling the same
services the bot uses.
